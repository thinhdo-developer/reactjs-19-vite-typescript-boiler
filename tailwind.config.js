/** @type {import('tailwindcss').Config} */
export default {
  // In Tailwind v4, most theme customizations are done in CSS using @theme
  // Only content paths and plugins are configured here
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  plugins: [],
};
