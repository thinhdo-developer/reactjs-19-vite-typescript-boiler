export const MIN_PASSWORD_LENGTH = 8;
export const MAX_LOGO_SIZE = 2 * 1024 * 1024; // 2MB

export const OTP_LENGTH = 4; // Length of OTP code
export const OTP_RESEND_TIME = 60; // Time in seconds to wait before allowing OTP resend

export const REDIRECT_URL_PARAM = "redirectUrl"; // Query parameter for redirect URL
export const FROM_URL_PARAM = "fromUrl"; // Query parameter for the source URL

export const TOAST_TIMEOUT = 3000; // Duration for toast messages in milliseconds
export const TOAST_POSITION = "top-right"; // Position for toast messages

export const TABLE_ITEMS_PER_PAGE = 10; // Default items per page for tables

export const TABLE_ITEMS_PER_PAGE_OPTIONS = [5, 10, 20, 50, 100, 200, 500]; // Options for items per page in tables
